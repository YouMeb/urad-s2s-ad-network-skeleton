{
  "name": "s2s-{{basename}}-ad-network",
  "version": "0.0.0",
  "description": "{{description}}",
  "main": "index.js",
  "scripts": {
    "lint": "make eslint",
    "test": "make test"
  },
  "pre-commit": [ "lint", "test" ],
  "dependencies": {
    "merge-descriptors": "^1.0.0",
    "s2s-ad-network": "^2.0.0"
  },
  "devDependencies": {
    "qs": "^2.4.2",
    "chai": "^2.0.0",
    "mocha": "^2.2.5",
    "urad-make-eslint": "^2.0.0",
    "precommit-hook": "^2.0.1"
  }
}
